<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Song Details</title>
    <link rel="stylesheet" href="/styles/songDetails.css">
</head>
<body>
    <div class="container" data-song-url="<%= song.url %>">
        <div class="header">
            <h1>Song Details</h1>
        </div>
        <div class="content">
            <div class="song-info">
                <img src="<%= song.picture %>" alt="Song Picture" class="song-picture" onerror="this.onerror=null;this.src='/images/default-song.png';">
                <div class="song-details">
                    <h2><%= song.title %></h2>
                    <p><strong>Artist:</strong> <%= song.artist %></p>
                    <p><strong>URL:</strong> <a href="<%= song.url %>" target="_blank"><%= song.url %></a></p>
                </div>
            </div>
            <div class="video-player">
                <div id="player"></div>
            </div>
            <div class="return-buttons">
                <a href="/playlists/<%= song.playlist_id %>" class="button">Back to Playlist</a>
            </div>
        </div>
    </div>
    <script src="https://www.youtube.com/iframe_api"></script>
    <script src="/scripts/songDetails.js"></script>
</body>
</html>